server:
  port: ${PORT:8080}
  servlet:
    context-path: /api

spring:
  datasource:
    url: mysql://root:DvUPOEaxzznpzBFjbDJGsKFaxXHTNzta@maglev.proxy.rlwy.net:17696/railway
    username: root
    password: DvUPOEaxzznpzBFjbDJGsKFaxXHTNzta
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

management:
  endpoints:
    web:
      exposure:
        include: "*"
